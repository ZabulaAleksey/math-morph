[build-system]
requires = ["hatchling>=1.27,<2"]
build-backend = "hatchling.build"

[project]
name = "math-morph-api"
version = "0.0.0"
description = "Пустой package будущего API adapter MathMorph"
requires-python = ">=3.12"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/math_morph_api"]
