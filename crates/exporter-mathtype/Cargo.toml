[package]
name = "exporter-mathtype"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
document-ir = { path = "../document-ir" }
exporter-mathml = { path = "../exporter-mathml" }
math-model = { path = "../math-model" }
