[package]
name = "exporter-mathml"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
document-ir = { path = "../document-ir" }
math-model = { path = "../math-model" }
thiserror.workspace = true
