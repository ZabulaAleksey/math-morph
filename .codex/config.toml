# Проектный overlay Codex для платформы преобразования и parsing Mathcad.
# Намеренно минимален, поскольку глобальная AI Dev Team Codex уже установлена.
#
# По умолчанию НЕ регистрируй здесь общие MCP servers или hooks.
# Сначала переиспользуй существующую глобальную или пользовательскую конфигурацию AI Dev Team.
# Необязательные проверенные фрагменты находятся в:
#   .codex/mcp.optional.toml
#   .codex/hooks.optional.toml
#
# Активными проектными subagents являются только специалисты домена Mathcad из .codex/agents/.
# Общие fallbacks остаются неактивными в .codex/agents-optional/.

# Ограничивай загрузку инструкций repository, чтобы уменьшить давление на контекст.
project_doc_max_bytes = 32768
